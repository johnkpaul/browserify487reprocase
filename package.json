{
  "name": "repro",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "bower install && (cd node_modules/browserify/node_modules/browser-pack/ && npm install)",
    "start": "node build > out.js && open index.html"
  },
  "dependencies": {
    "browserify-shim": "~2.0.8",
    "moment": "~2.2.1",
    "browserify": "thlorenz/node-browserify#fix-dedupe-sourcemaps"
  },
  "devDependencies": {
    "bower": "~1.2.6"
  }
}
